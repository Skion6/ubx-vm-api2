const e=require('express'),f=(...a)=>import('node-fetch').then(({default:r})=>r(...a));const p=require('path');const a=e();const P=process.env.PORT||3000;const EC2_BASE_URL='https://ec2-18-219-58-159.us-east-2.compute.amazonaws.com';const EC2_HOST='ec2-18-219-58-159.us-east-2.compute.amazonaws.com';const CREATE_VM_URL=`${EC2_BASE_URL}/api/create?developer_id=your_name&site_limit=10&delete_after=60`;const vmStore=new Map();a.use(e.json());a.use(e.static('public'));async function checkEC2Health(vmUrl,maxRetries=30,delayMs=1e3){for(let i=0;i<maxRetries;i++){try{const r=await fetch(vmUrl,{method:'HEAD',timeout:5e3});if(r.status===200)return{healthy:!0,url:vmUrl};console.log(`Attempt ${i+1}: Got status ${r.status}, retrying...`)}catch(t){console.log(`Attempt ${i+1}: Error - ${t.message}, retrying...`)}if(i<maxRetries-1)await new Promise(r=>setTimeout(r,delayMs))}return{healthy:!1,url:vmUrl}}a.get('/api/vm',async(r,s)=>{try{console.log('Creating VM...');const premiumCode=r.query.premium||'';let apiUrl=CREATE_VM_URL;if(premiumCode)apiUrl+=`&premium=${encodeURIComponent(premiumCode)}`;const response=await fetch(apiUrl,{method:'GET',headers:{Accept:'application/json'}});if(!response.ok){const t=await response.text();return s.status(response.status).json({error:'Failed to create VM',details:t})}const vmData=await response.json();console.log('VM created:',vmData);if(vmData.status!=='success')return s.status(500).json({error:'VM creation failed',details:vmData});const vmUrl=vmData.url,containerId=vmData.container_id;vmStore.set(containerId,{...vmData,createdAt:Date.now()});console.log(`Checking VM health at ${vmUrl}...`);const healthCheck=await checkEC2Health(vmUrl);if(!healthCheck.healthy)return s.json({...vmData,warning:'VM may still be starting up. Please refresh if you see a 502 error.',healthCheckAttempts:30});return s.json({...vmData,verified:!0})}catch(t){console.error('Error creating VM:',t);return s.status(500).json({error:'Failed to create VM',details:t.message})}});a.get('/api/connect/:port',async(r,s)=>{const{port}=r.params;const vmUrl=`http://${EC2_HOST}:${port}/`;try{console.log(`Manually connecting to port ${port}...`);const containerId=`manual-${port}`;console.log(`Checking health for manual port ${port} at ${vmUrl}...`);const healthCheck=await checkEC2Health(vmUrl);const vmData={status:'success',url:vmUrl,container_id:containerId,port:port,name:`Friend's Session (${port})`,verified:healthCheck.healthy};vmStore.set(containerId,{...vmData,createdAt:Date.now(),max_session_minutes:60,inactivity_timeout_seconds:60});return s.json(vmData)}catch(t){console.error('Error connecting to port:',t);return s.status(500).json({error:'Failed to connect to port',details:t.message})}});a.get('/api/vm/:containerId',async(r,s)=>{const{containerId}=r.params;const vmData=vmStore.get(containerId);if(!vmData)return s.status(404).send(`<!DOCTYPE html><html><head><title>VM Not Found</title><style>body{font-family:Arial,sans-serif;padding:40px;text-align:center}.error{color:red}</style></head><body><h1 class="error">VM Not Found</h1><p>The requested VM container ID was not found.</p><p><a href="/">Go to Home</a></p></body></html>`);const vmUrl=vmData.url,maxSessionMinutes=vmData.max_session_minutes||60,inactivityTimeoutSeconds=vmData.inactivity_timeout_seconds||60,name=vmData.name||containerId;const html=`<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"><title>VM Console | ${name}</title><link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap" rel="stylesheet"><style>:root{--bg-color:#0a0a0d;--header-bg:#12121a;--primary-color:#ff6b00;--text-main:#e1e1e6;--text-muted:#9494a3;--border-color:rgba(255,107,0,.15)}*{margin:0;padding:0;box-sizing:border-box}body{font-family:'Outfit',sans-serif;background:var(--bg-color);color:var(--text-main);height:100vh;display:flex;flex-direction:column;overflow:hidden}.header{background:var(--header-bg);padding:12px 24px;display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid var(--border-color);z-index:20}.vm-brand{display:flex;align-items:center;gap:12px}.logo-small{width:24px;height:24px;background:var(--primary-color);border-radius:6px;display:flex;align-items:center;justify-content:center;color:#000;font-weight:800;font-size:14px}.vm-name{font-size:14px;font-weight:600;color:var(--text-main)}.vm-id-tag{font-size:11px;color:var(--text-muted);background:rgba(255,255,255,.03);padding:2px 8px;border-radius:4px;font-family:monospace}.controls{display:flex;align-items:center;gap:24px}.timer-box{display:flex;align-items:center;gap:8px;font-family:monospace;font-weight:700;color:var(--primary-color);font-size:16px}.timer-box.warning{color:#ff5252;animation:pulse 1s infinite}@keyframes pulse{0%,100%{opacity:1}50%{opacity:.6}}.inactivity-status{font-size:12px;color:var(--text-muted)}.vm-viewport{flex:1;position:relative;background:#000}iframe{width:100%;height:100%;border:none;position:absolute;top:0;left:0}.overlay{position:absolute;top:0;left:0;right:0;bottom:0;background:var(--bg-color);display:flex;flex-direction:column;justify-content:center;align-items:center;z-index:10;gap:20px}.loader{width:40px;height:40px;border:3px solid rgba(255,107,0,.1);border-top:3px solid var(--primary-color);border-radius:50%;animation:spin 1s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}</style></head><body><div class="header"><div class="vm-brand"><div class="logo-small">U</div><span class="vm-name">${name}</span><span class="vm-id-tag">${containerId.substring(0,12)}</span></div><div class="controls"><span class="inactivity-status">⚠️ Inactivity Timeout (${inactivityTimeoutSeconds}s)</span><div class="timer-box" id="timer">${maxSessionMinutes}:00</div></div></div><div class="vm-viewport"><div class="overlay" id="loading"><div class="loader"></div><p style="font-size:13px;color:var(--text-muted)">Attaching to workspace...</p></div><iframe id="vmFrame" src="${vmUrl}" allow="fullscreen; clipboard-read; clipboard-write" sandbox="allow-scripts allow-same-origin allow-forms allow-popups allow-downloads"></iframe></div><script>let totalSeconds=${maxSessionMinutes}*60;const timerEl=document.getElementById('timer');const updateTimer=()=>{const m=Math.floor(totalSeconds/60),s=totalSeconds%60;timerEl.textContent=m+':'+(s<10?'0':'')+s;if(totalSeconds<=300)timerEl.classList.add('warning');if(totalSeconds<=0){timerEl.textContent='EXPIRED';document.getElementById('vmFrame').style.display='none';document.getElementById('loading').style.display='flex';document.getElementById('loading').innerHTML='<h2 style="color:#ff5252">Session Expired</h2>';return}totalSeconds--};setInterval(updateTimer,1000);const iframe=document.getElementById('vmFrame'),loading=document.getElementById('loading');iframe.onload=()=>loading.style.display='none';iframe.onerror=()=>loading.innerHTML='<h2 style="color:#ff5252">Failed to load environment</h2>';</script></body></html>`;s.setHeader('Content-Type','text/html');s.setHeader('X-Frame-Options','SAMEORIGIN');s.send(html)});a.get('/',(r,s)=>{s.sendFile(p.join(__dirname,'public','index.html'))});a.listen(PORT,()=>{console.log(`Server running on http://localhost:${PORT}`);console.log(`API endpoint: http://localhost:${PORT}/api/vm`)});
